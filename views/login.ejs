<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TrendHive - Login</title>
    <link rel="stylesheet" href="/css/reset.css">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }
        
        .login-container {
            width: 100%;
            max-width: 400px;
            padding: 2rem;
        }
        
        .login-card {
            text-align: center;
        }
        
        .app-title {
            margin-bottom: 2rem;
            font-size: 1.75rem;
            font-weight: 700;
            color: var(--primary);
        }
        
        .login-form {
            display: flex;
            flex-direction: column;
        }
        
        .login-input {
            margin-bottom: 1rem;
            padding: 0.75rem 1rem;
            border-radius: 8px;
            border: 1px solid var(--border-color);
            font-size: 1rem;
        }
        
        .login-button {
            margin-top: 1rem;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="card login-card">
            <h1 class="app-title">TrendHive Stock Analysis</h1>
            
            <form action="/login" method="post" class="login-form">
                <input type="email" name="email" required placeholder="Enter your email" class="login-input">
                <button type="submit" class="button login-button">Login</button>
            </form>
            
            <% if (message) { %>
                <div class="message message-error" style="margin-top: 1rem;">
                    <%= message %>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>
